import React from 'react';
// Import do DS (pode ser importado via `import { Logo } from 'design-system'` se estiver resolvendo local).
import { Logo } from 'design-system';

const MFELogo: React.FC = () => {
  return <Logo alt="Logo Mercado Libre" />;
};

export default MFELogo;
